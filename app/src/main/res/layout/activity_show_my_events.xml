<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/activity_show_my_events"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context="com.example.ofir.bopofinal.Events.ShowMyEventsActivity">

    <Button
        android:text="Participant"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:id="@+id/btnParticipant"
        android:layout_centerVertical="true"
        android:layout_alignStart="@+id/btnWaitingForAck"
        android:layout_alignEnd="@+id/btnWaitingForAck"
        android:onClick="onClick" />

    <Button
        android:text="Waiting for ack"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@+id/btnParticipant"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="38dp"
        android:id="@+id/btnWaitingForAck"
        android:onClick="onClick" />

    <Button
        android:text="Moderator"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:id="@+id/btnModerator"
        android:layout_above="@+id/btnParticipant"
        android:layout_alignStart="@+id/btnParticipant"
        android:layout_marginBottom="39dp"
        android:layout_alignEnd="@+id/btnParticipant"
        android:onClick="onClick"/>

    <TextView
        android:text="Show events where I am:"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentTop="true"
        android:layout_centerHorizontal="true"
        android:textSize="18dp"
        android:textStyle="bold"
        android:layout_marginTop="37dp"
        android:id="@+id/tvShowMyEvents" />
</RelativeLayout>
